const routeModel = require('./route');
const stoppageModel =  require('./stoppage');
const vehicleModel = require('./vehicle');
const vehicleRouteModel = require('./vehicleRoute');

module.exports = {
    routeModel,
    stoppageModel,
    vehicleModel,
    vehicleRouteModel
};